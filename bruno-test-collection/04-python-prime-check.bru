meta {
  name: 04 - Python Prime Check
  type: http
  seq: 4
}

post {
  url: http://localhost:3000/execute
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Execution-Secret: codemania-secret-key-2026
}

body:json {
  {
    "code": "def is_prime(n):\n    if n < 2:\n        return False\n    for i in range(2, int(n**0.5) + 1):\n        if n % i == 0:\n            return False\n    return True\n\nn = int(input())\nprint('YES' if is_prime(n) else 'NO')",
    "language": "python",
    "testCases": [
      {
        "input": "7",
        "expectedOutput": "YES"
      },
      {
        "input": "10",
        "expectedOutput": "NO"
      },
      {
        "input": "13",
        "expectedOutput": "YES"
      }
    ],
    "timeLimit": 2000,
    "submissionId": "test-lb-004"
  }
}
